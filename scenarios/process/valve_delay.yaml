schema_version: "0.1"
scenario_id: valve_delay
scenario_family: process_valve_fault
description: Feed valve response is delayed for a bounded interval.
process_config: configs/process/cstr_baseline.yaml
steps: 8
faults:
  - fault_id: feed-valve-delay-001
    fault_type: valve_delay
    start_step: 2
    duration: 5
    target_asset: feed_valve
    parameters:
      delay_steps: 2
    label: feed valve response delay
ground_truth:
  incident_class: process_fault
  severity: medium
allowed_actions:
  - inspect_process_state
  - verify_sensor_health
  - compare_redundant_measurements
forbidden_actions:
  - change_process_setpoint
  - write_to_plant_or_network
  - execute_shell_command
